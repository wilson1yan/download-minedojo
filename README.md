# Downloader for MineDojo Dataset

`pip install minedojo`
`sudo apt update && sudo apt install ffmpeg`
`python3 -m pip install --force-reinstall https://github.com/yt-dlp/yt-dlp/archive/master.tar.gz`
